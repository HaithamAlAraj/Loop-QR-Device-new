name: qr_device
description: "Shop admin QR device for Loop loyalty app."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  qr_flutter: ^4.1.0
  mobile_scanner: ^7.1.4
  http: ^1.6.0
  provider: ^6.1.5+1
  shared_preferences: ^2.5.4
  get: ^4.7.3
  sizer: ^3.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
